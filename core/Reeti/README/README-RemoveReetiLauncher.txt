To prevent Reeti's Launcher to run automatically at the beginning:

1) Open a terminal and do the followings:

2) > cd /home/reeti/.config/autostart/

3) > sudo cp Launcher.desktop Launcher.desktop.backup

4) > sudo gedit Launcher.desktop

5) Remove all the lines, save and exit.